import { atom } from "recoil";

export const reservation = atom({
  key: "reservation",
  default: {},
});
export const reserveList = atom({
  key: "reserveList",
  default: {},
});
export const reserveAllPrice = atom({
  key: "reserveAllPrice",
  default: {},
});
